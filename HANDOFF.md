# 引き継ぎ書 — エスパス/秋葉原 スロット狙い台システム

このファイル1枚で、**別PC（自宅用PC）や別のClaudeセッションでも作業を完全に引き継げる**ようにまとめています。まずここを読めば「目的・現状・次にやること・実行手順」が全部わかります。

---

## 0. 超要約（いま何をすべきか）
- みんレポ（min-repo.com）から4店のスロット全台データを**毎日1回**取得し、`data/日付_店.csv` に蓄積する。
- **GitHub ActionsはサイトにIPブロックされ使えない**（全リクエストが200/402バイトのブロックページ）。→ **自宅PCで `collect.py` を実行する方針に切替済み**（住宅IPならブラウザ同様に取得できる）。
- まずは **自宅PCでPython導入 → `python collect.py` が全台取れるか確認 → タスクスケジューラで毎日自動化**。手順は §5。

## 1. 目的
エスパス日拓上野（新館・本館）と秋葉原2店のジャグラー（＋全スロット）の出玉データを蓄積し、**任意の日の「狙い台」を統計的に絞り込む**。将来は日付タイプ（曜日・末尾・0/7/ゾロ目）ごとの傾向学習まで。的中保証の装置ではなく、確率的な優位を見つける補助ツール。

## 2. 現状サマリー
- GitHubリポジトリ：`dzknrts-ctrl/espace-slot-data`（Private）にコード＋データ＋本書あり。
- 収集方式は **「末尾別 ?kishu=0〜9 で全台取得 ＋ 機種名は機種ページで付与」** に確定（機種ナビ依存だと約半分しか取れなかったため）。
- コレクター `collect.py` は完成（末尾別・機種付与・直近5日バックフィル・本物UA・リトライ・部分成功でも失敗にしない）。**コード自体は正常**。
- **唯一の障害＝実行環境のIPブロック**。→ 自宅PC実行で回避する。
- 収集済みデータの一部（旧ロジック時代の上野8/14〜18）は**不完全（全台の約半分）**。全台方式の稼働を確認したら削除して取り直す。

## 3. データソース / 収集仕様（重要な既知情報）
- サイト：**みんレポ（min-repo.com）**。自動取得を禁じていない唯一の実用サイト（規約検証済み）。**個人・非公開利用前提／1日1回・低頻度・適切なUA**。
- 対象4店（コード上のkey｜正式名｜全台数の目安＝「勝率」の分母）：
  | key | 店名 | 全台数 | みんレポtag |
  |---|---|---|---|
  | `shinkan` | エスパス日拓上野新館 | 377 | エスパス日拓上野新館 |
  | `honkan` | エスパス日拓上野本館 | 375 | エスパス日拓上野本館 |
  | `island_akiba` | アイランド秋葉原店 | 395 | アイランド秋葉原店 |
  | `espace_akiba` | エスパス日拓秋葉原駅前店 | 522 | エスパス日拓秋葉原駅前店 |
- URL構造：
  - 記事は `https://min-repo.com/{postid}/`。postidは**1日あたり約2000ずつ増加**。
  - タグ一覧 `https://min-repo.com/tag/{店名}/` の最新側が現行年（**ページ送りは前年データが混じるので信用しない**）。日付・館は記事タイトル `M/D(曜) 店名 | …` で検証する。
  - **全台データ**＝末尾別：`/{postid}/?kishu=0` 〜 `?kishu=9`。各ページは `台番/差枚/G数/出率/BB/RB/合成/BB率/RB率` の全台表。10ページで全台を漏れなく網羅（各台番号は末尾1桁に1回だけ出る）。ゾロ目 `?kishu=z` は重複なので**取らない**。
  - **機種名**＝`/{postid}/?kishu=<機種名>`（URLエンコード）で per-台番号表。ここから `台番→機種` を付与。付かない台は「不明」でもBB/RB等のデータは完全。
  - 全台数の検算：記事の「勝率 x/y」の **y = その日の全台数**。`wrote NNN rows (全台 NNN/377 …)` が分母どおりなら網羅成功。

## 4. コレクター `collect.py`
- Python（`requests` + `beautifulsoup4`）。
- 使い方：
  ```
  python collect.py                # 直近5日で未取得の日を全台収集（既定）
  python collect.py --date 8/18    # 指定日(当年)だけ
  python collect.py --days 10      # 遡り日数
  python collect.py --juggler-only # ジャグラー系のみ
  python collect.py --force        # 既存でも再取得（取り直し用）
  ```
- 出力：`data/YYYY-MM-DD_{hall}.csv`（列 `date,hall,model,daban,G,BB,RB,samai`）。
- ※ `hall` は英字（shinkan等）、`date` は `2026-08-18` 形式で保存。ブラウザの自動翻訳で日本語に見えることがあるが中身は英字。

## 5. 自宅PCでのセットアップ手順（Windows）
1. **Pythonを入れる**：https://www.python.org/downloads/ から最新版をDL → インストーラで**「Add python.exe to PATH」に必ずチェック** → Install。
2. **コード一式を自宅PCに置く**：GitHubのリポジトリ画面 → 緑の **Code → Download ZIP** → 解凍。または `git clone`。`collect.py` があるフォルダを使う。
3. **依存ライブラリを入れる**：フォルダで「アドレスバーに `cmd` と打ってEnter」→ コマンドプロンプトで：
   ```
   pip install requests beautifulsoup4
   ```
4. **手動テスト**：同じコマンドプロンプトで：
   ```
   python collect.py --date 8/18
   ```
   → `data/` にCSVができ、ログに `wrote NNN rows (全台 NNN/377 / 機種判明 …)` が**分母どおり**なら成功（自宅IPなのでブロックされないはず）。
5. **毎日自動化（タスクスケジューラ）**：同梱の `run_collect.bat` を使う。
   - タスクスケジューラを開く → 「基本タスクの作成」→ 名前「juggler collect」→ トリガー「毎日」時刻を指定（例 昼12時。前日分を取る）→ 操作「プログラムの開始」→ `run_collect.bat` を選択 → 完了。
   - 実行結果は同フォルダの `run.log` に追記される。
   - PCが起動していない時刻は実行されないので、タスクのプロパティで「スケジュールされた時刻にタスクを開始できなかった場合、すぐにタスクを開始する」にチェックしておくと取りこぼしにくい（`collect.py`側も直近5日バックフィルで穴埋めする）。

## 6. 分析ロジック（Phase 2で本実装）
- 台ごとに **BB回数・RB回数** から、機種の**設定別テーブル**に対して**ベイズ推定（ポアソン尤度・一様事前）**で設定の確率分布を出し、**期待機械割**→**優位度**を算出（累積Gが少ない台は自然に平均へ回帰）。
- 設定テーブル（BB間隔/RB間隔/機械割）と機械割は**マイジャグラー基準の暫定値**。機種ごとの正式値に差し替えると精度が上がる。
- **狙いは「島（シマ）」単位が主**。日付タイプ別（曜日・末尾・0/7/ゾロ目）に「どの島に高設定が入るか」を履歴学習して任意の日に対応する。
- **8/15バックテストの知見**：本館アイム島＝据わりが固い鉄板（当日も77%が設定4+）。新館は島が日替わりで振れる（マイ⇄アイム）。**個別台より島。単日の台推定はノイズが大きい**（島平均は大母数で信頼度高）。

## 7. 次にやること（ロードマップ）
- **Phase 1（いま）**：自宅PCで日次収集を軌道に乗せる。`全台 NNN/分母` が一致するのを確認。
- 旧ロジックの**不完全CSV（上野8/14〜18）を削除→取り直し**（`--date` か `--force`）。
- **Phase 2**：日付タイプ別の本命シマ学習 → 5の日に限らず任意の日の狙い台レポート。
- **Phase 3**：当日リアルタイム判定（日中に複数回取得する別スケジュール）。
- スマホ用の狙い台ダッシュボード（過去に試作あり）を、貯まったデータで生成。

## 8. 制約・厳守事項
- **自動取得を禁止しているサイトは使わない**（P-WORLD＝規約で禁止／台データオンライン＝bot遮断。いずれも使用不可）。**みんレポのみ**。
- 参考情報であり**的中・勝敗を保証しない**。**個人・非公開利用**。規約は変わり得るので定期的に再確認。

## 9. リポジトリ内ファイル
- `collect.py` … 収集本体（末尾別・全台・機種付与）。
- `run_collect.bat` … 自宅PCのタスクスケジューラ用（ログを `run.log` に出す）。
- `.github/workflows/daily.yml` … GitHub Actions用。**IPブロックで現在は使わない**（将来プロキシ等で復活する場合の残置）。
- `data/` … 日別・店別CSV。
- `README.md` / `HANDOFF.md`（本書）。

---
**引き継ぎのゴール**：この文書を読んだ人（またはClaude）が、自宅PCで `collect.py` を毎日走らせて全台データを貯め、Phase 2の分析に進める状態。まずは §5 のセットアップから。
